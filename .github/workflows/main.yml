name: firtsworkflow


on: push

jobs:
     build: 
     
      runs-on: ubuntu-latest


     steps: 
         - uses: actions/checkout@v3
         - name: run a sample gitgub
           run: echo "hi"

